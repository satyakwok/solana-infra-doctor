# Real-run evidence matrix

Every documented `sol-doctor` feature was executed against a real endpoint, or is
explicitly marked **BLOCKED** because it requires private credentials. No output
in this repository is mocked, hand-edited, or fabricated.

- Verified: see [`last-verified.md`](last-verified.md) (timestamp, version, commit).
- Exact commands: see [`commands-used.md`](commands-used.md).
- Evidence files carry a header with the exact command, UTC timestamp, version,
  commit, and endpoint label.

| Feature | Real public run | Evidence | Notes |
| --- | --- | --- | --- |
| HTTP `check` | ✅ Yes | [`examples/terminal/check-mainnet.txt`](../../examples/terminal/check-mainnet.txt), [`check-devnet.txt`](../../examples/terminal/check-devnet.txt) | mainnet + devnet, both GOOD |
| `check --data` (gPA + archival) | ✅ Yes | [`check-data-mainnet.txt`](../../examples/terminal/check-data-mainnet.txt) | getProgramAccounts ready, full archival |
| `check --json` | ✅ Yes | [`check-json-mainnet.txt`](../../examples/terminal/check-json-mainnet.txt) | valid JSON, 11 checks |
| `check --samples` (p50/p95) | ✅ Yes | [`check-samples-mainnet.txt`](../../examples/terminal/check-samples-mainnet.txt) | 10 samples |
| HTTP `compare` (profiles) | ✅ Yes | [`compare-mainnet-publicnode.txt`](../../examples/terminal/compare-mainnet-publicnode.txt) | `bot` profile, both GOOD |
| `compare --data` (indexer) | ✅ Yes | [`compare-data-indexer.txt`](../../examples/terminal/compare-data-indexer.txt) | indexer data-readiness scoring |
| `compare --report` (Markdown) | ✅ Yes | [`examples/reports/compare-indexer-data-live.md`](../../examples/reports/compare-indexer-data-live.md) | report generated by the real CLI |
| WebSocket `ws` (slotSubscribe) | ✅ Yes | [`ws-mainnet.txt`](../../examples/terminal/ws-mainnet.txt) | mainnet WS |
| `ws --subscription logs` | ✅ Yes | [`ws-logs-mainnet.txt`](../../examples/terminal/ws-logs-mainnet.txt) | logsSubscribe |
| Redaction-safe output | ✅ Yes | all files above (public endpoints, no credentials) + redaction unit/integration tests | no secret printed |
| GitHub Action (`@v1`) | ✅ Yes (CI) | [`.github/workflows/action-selftest.yml`](../../.github/workflows/action-selftest.yml) | CI runs the action against a public RPC on every push/PR |
| Yellowstone **`grpc check`** | ⛔ Blocked | [`grpc-check-blocked-private-endpoint-required.txt`](../../examples/terminal/grpc-check-blocked-private-endpoint-required.txt) | implemented; needs a private `x-token` (no stable public no-token endpoint) |
| Yellowstone **`grpc compare`** | ⛔ Blocked | same | implemented; needs private credentials |

## Notes on coverage

- **Compare profiles** (`general`, `wallet`, `bot`, `indexer`, `ci`): the `compare`
  command path is exercised by real runs (`bot` and `indexer` above). Profiles are
  deterministic scoring branches over the same real per-endpoint data.
- **Public-endpoint reality**: public RPCs may rate-limit rapid repeated probes.
  Any degraded result captured during evidence collection is a real public-endpoint
  condition, recorded honestly — not treated as a healthy example.
- **gRPC**: every major Yellowstone provider requires a private `x-token`, and there
  is no stable public no-token endpoint. The commands are documented with
  redaction-safe usage; run them with your own endpoint and token (see the blocked
  evidence file).

## Honest claim

**All public-endpoint features have successful real-run evidence. Yellowstone gRPC
is implemented but blocked from a public real run because it requires private
credentials.**

This repository does **not** claim "all tools use real data" without qualification,
because gRPC cannot be validated against a public endpoint.
