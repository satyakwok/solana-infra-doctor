# Assets

Branding and image assets (logo, social preview, etc.) used in the rendered
README and GitHub pages — **not** shipped in the published crate (this directory
is listed under `exclude` in `Cargo.toml`).

Upload the logo here as `logo.png` (or `logo.svg`).
